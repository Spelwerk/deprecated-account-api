const loginPath = '/login/password';
const administrator = {
    email: 'admin@spelwerk.com',
    password: 'password',
};
const keys = {
    accessToken: 'x-access-token',
    refreshToken: 'x-refresh-token',
};

module.exports = {
    loginPath,
    administrator,
    keys,
};
